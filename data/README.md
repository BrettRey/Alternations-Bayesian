# Data

Expected layout:
- `data/raw/oanc/`: raw OANC text files (exported as `.txt`).
- `data/processed/`: parquet outputs from the analysis pipeline.

Record sources and licenses in a `DATA_README.md` once the corpus is downloaded.
